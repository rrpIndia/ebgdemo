{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "build-ghpages": "eleventy --pathprefix=/website/ && npx pagefind --site _site",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-navigation": "^0.3.5",
    "pagefind": "^1.0.4"
  }
}
