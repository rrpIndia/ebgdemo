{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prebuild": "chmod +x node_modules/.bin/pathfind",
    "build": "eleventy",
    "build-ghpages": "npx eleventy --pathprefix=/website/ && npx pagefind --site _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-navigation": "^0.3.5",
    "pagefind": "^1.0.0"
  }
}
